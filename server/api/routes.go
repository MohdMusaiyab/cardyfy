package api

import (
	"net/http"

	// "github.com/MohdMusaiyab/cardyfy/handlers"
)

func RegisterRoutes(mux *http.ServeMux) {
	// mux.HandleFunc("/api/generate-image", handlers.GenerateImageHandler())
	// mux.HandleFunc("/api/image/", handlers.ServeImageHandler)
	// mux.HandleFunc("/api/short-url/", handlers.ShortURLHandler)
	// mux.HandleFunc("/api/meta/", handlers.MetaHandler)
}
